# Agent Instructions

Read `README.md`, `test.py`, and `poro_perm.csv` before planning. Explain the class contracts and proposed implementation, then wait for approval before editing code.

For the instruction-review exercise only, propose a compact method-specific addition to `AGENTS.md` without saving it. Wait for the student's explicit approval of the proposed diff before applying it. Never silently modify governing instructions or the provided skill.

During implementation, edit only `assignment6.py`. Do not edit `README.md`, `test.py`, `poro_perm.csv`, `AGENTS.md`, `environment.yml`, `.gitignore`, or anything under `.github/` or `.devcontainer/`.

Run `python -m unittest -v` and `git diff --check` after implementation. Stop and report failures or unexpected changes; never bypass tests or weaken instructions. Make no instruction changes during implementation or submission.

When the user says `submit assignment 6`, load and follow `.github/skills/submit-assignment/SKILL.md` (the `submit-assignment` skill).
